#   Physically Based Rendering

##  Cloning

-   Use the following command:

```sh
git clone --recursive <url>
```

##  Compilation and Execution

```sh
cmake -B build -S .
cmake --build build

bin/pbr
```


